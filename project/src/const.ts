export enum AppRoute {
  Favorites = '/favorites',
  Login = '/login',
  Root = '/',
  Room = '/offer/:id',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
